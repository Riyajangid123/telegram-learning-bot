services:
  - type: web
    name: telegram-learning-bot
    env: docker
    plan: free
    startCommand: python main.py & python -m http.server $PORT
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: TELEGRAM_BOT_TOKEN
        sync: false