services:
  - type: web
    name: telegram-learning-bot
    env: docker
    plan: free
    maxInstances: 1               
    deploymentStrategy: recreate
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: TELEGRAM_BOT_TOKEN
        sync: false