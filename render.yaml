services:
  - type: web
    name: telegram-learning-bot
    env: docker
    plan: free
    scaling:                  
      maxInstances: 1        
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: TELEGRAM_BOT_TOKEN
        sync: false