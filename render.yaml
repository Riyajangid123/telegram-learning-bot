services:
  - type: web
    name: telegram-learning-bot
    env: docker
    plan: free
    scaling:                  
      maxInstances: 1        
    deployWithWithOverlaps: false
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: TELEGRAM_BOT_TOKEN
        sync: false