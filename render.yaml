services:
  - type: worker
    name: telegram-learning-bot
    env: docker
    region: oregon 
    plan: free
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: DATABASE_URL
        sync: false